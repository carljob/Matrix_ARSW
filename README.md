Proyecto Java - Estructura de scaffolding

Este repositorio contiene únicamente la estructura de carpetas (scaffolding) para un proyecto Maven/Java.

Estructura creada:

- src/
  - main/
    - java/com/matrix/simulation/  (vacío)
    - resources/                   (vacío)
  - test/
    - java/com/matrix/simulation/  (vacío)
    - resources/                   (vacío)
- docs/      (vacío)
- config/    (vacío)
- pom.xml    (presente en la raíz)
- .gitignore

Instrucciones rápidas:
- Para compilar (si se añade código), ejecutar: mvn -q -DskipTests package


